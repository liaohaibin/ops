apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc1
  namespace: mysql-cluster2
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-mysql